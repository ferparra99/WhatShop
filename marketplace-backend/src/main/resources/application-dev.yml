spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-secret-whatshop-256-bits-clave-local-123456}

logging:
  level:
    com.whatshop.marketplace: DEBUG
    org.springframework.security: DEBUG
